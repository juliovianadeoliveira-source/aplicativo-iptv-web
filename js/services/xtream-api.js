export class XtreamApi{constructor(session){this.session=session}url(action='',extra={}){const u=new URL('player_api.php',this.session.server.replace(/\/+$/,'')+'/');u.searchParams.set('username',this.session.username);u.searchParams.set('password',this.session.password);if(action)u.searchParams.set('action',action);Object.entries(extra).forEach(([k,v])=>u.searchParams.set(k,v));return u}async request(action='',extra={}){const r=await fetch(this.url(action,extra),{cache:'no-store',mode:'cors',credentials:'omit'});if(!r.ok)throw new Error(`HTTP ${r.status}`);return r.json()}live(id,ext='m3u8'){return `${this.session.server.replace(/\/+$/,'')}/live/${encodeURIComponent(this.session.username)}/${encodeURIComponent(this.session.password)}/${id}.${ext}`}}
